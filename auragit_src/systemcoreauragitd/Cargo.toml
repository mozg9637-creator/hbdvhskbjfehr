[package]
name = "auragitd"
version = "1.0.0"
edition = "2021"

[dependencies]
nix = "0.27" # Для работы с системными вызовами Linux/Android
tokio = { version = "1.0", features = ["full"] } # Асинхронный движок